# TicketFlo Redirects and SPA Configuration

# Apple App Site Association - must be served as-is (before SPA catch-all)
/.well-known/*  /.well-known/:splat  200

# Handle SPA routing - must be last rule
/*    /index.html   200

# Optional: Remove trailing slashes to prevent duplicate content
/*/   /:splat       301

# Force HTTPS
http://www.ticketflo.org/*   https://www.ticketflo.org/:splat   301!
http://ticketflo.org/*       https://www.ticketflo.org/:splat   301!

# Force www domain
https://ticketflo.org/*      https://www.ticketflo.org/:splat   301!